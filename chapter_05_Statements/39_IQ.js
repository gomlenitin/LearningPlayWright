// we can run one statement without brackets
let x = 10;
if (x > 5)
    console.log("x is big");

// we cannot run two statement without brackets
if (x > 5) {
    console.log("x is big");
    console.log("x is big");
}
